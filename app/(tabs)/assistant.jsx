import { FlatList, StyleSheet, View } from "react-native";

const Assistant = () => {
  return (
    <View className="flex-1 bg-[#000022]">
      <FlatList />
    </View>
  );
};

export default Assistant;

const styles = StyleSheet.create({});
