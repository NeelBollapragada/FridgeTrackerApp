import { StyleSheet, Text, View } from "react-native";

const scanner = () => {
  return (
    <View>
      <Text>scanner</Text>
    </View>
  );
};

export default scanner;

const styles = StyleSheet.create({});
