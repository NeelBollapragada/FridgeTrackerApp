import { StyleSheet, Text, View } from "react-native";

const FridgeCard = ({ food_item }) => {
  return (
    <View>
      <Text>FridgeCard</Text>
    </View>
  );
};

export default FridgeCard;

const styles = StyleSheet.create({});
